This directory holds the base files for PoolingScreen

Future updates are likley to contain config files for easier use of the pipeline


The recommended computing resources would be to use a high resource computing cluster. 16 cores and 300 GB of ram are a god starting point as the pipeline includes assembly. Depending on the size of the raw sequencing dataset, this may influence the computing resources required. 
